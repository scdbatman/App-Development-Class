//  🏔️ MTECH Code Challenge PC15: "Simplifying Fractions"
//  Concept: Practice manipulating numbers to represent them in a human readable format

//  Instructions:
    //  Create a function which takes a string as input and returns a string as well. The function's input will be a fraction, represented as "6/10", for example. Simplify the fraction into its simplest ratio. Return the simplified fraction as a string.

//  Notes:
    //  If the input is not a valid fraction, return an empty string.

//  Examples:
    //  Input: simplifyFraction("2/4")
    //  Output: "1/2"

    //  Input: simplifyFraction("15/25")
    //  Output: "3/5"

    //  Input: simplifyFraction("4/9")
    //  Output: "4/9"

//  ⌺ Black Diamond Challenge:
    //  Extend the simplifyFraction function to also handle mixed numbers and improper fractions.
        //  A mixed number is a whole number followed by a fraction, e.g., "2 1/2".
        //  An improper fraction is a fraction where the numerator is greater than or equal to the denominator, e.g., "7/4".
    //  If the input is an improper fraction, convert it to a mixed number. If the input is a mixed number, convert it to an improper fraction and then simplify.
    //  If the input is not a valid fraction or mixed number, return an empty string.

import Foundation
